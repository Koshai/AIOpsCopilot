import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Configure workspace preferences, API connections, and platform defaults."
      panelTitle="Workspace settings"
      panelDescription="Environment and integration controls will be configured here."
    />
  );
}
